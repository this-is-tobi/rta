# Dockerfile.full's ARG PLUGINS pins each first-party plugin to its own
# rta-plugins release tag by hand — see the comment above that line for why
# that is deliberate: the image is the plugin allowlist, and a plugin joins
# it by somebody writing its line here. What was never deliberate is the
# version staying still after rta-plugins ships a newer one: nothing bumped
# it, so the pin only ever drifted further behind.
#
# This closes that the same direction rta-plugins' own bump-rta.yml closes
# the opposite one: poll, diff, open a pull request, let CI be the review.
# The names come from the official index — index/<name>.yaml in rta-plugins,
# the manifests `rta plugin index add official` reads — so a plugin that
# enters the index joins this image on the next run, at the version its
# manifest names, and one already here follows its releases. A name on the
# line that the index does not know is kept exactly as written: an image
# built from this file may carry more than the official set, and that
# addition is somebody's to make and to maintain. Nothing leaves the line but
# by hand — a name that dropped out of the index is kept and called out,
# because a fetch that came back short must not narrow the image on its own.
#
# Polling, on purpose, and the same reasoning as bump-rta.yml: reading
# rta-plugins' public releases needs no credential at all, and a push from
# rta-plugins' own release workflow would need a token here that could act on
# this repository — which is a credential this repository otherwise has no
# reason to hand out. workflow_dispatch is the zero-delay path when somebody
# is watching.
#
# The pull request is opened as the App rather than with GITHUB_TOKEN: a pull
# request GITHUB_TOKEN opens gets no CI run on it, and an image built from a
# pin nobody has actually built is exactly the change that must not merge on
# trust. ci.yml builds the full image for a pull request that touches
# Dockerfile.full — this one, and hardly any other — so the check is the
# build itself, not a proxy for it.
#
# A merged bump is not an rta release: release-please-config.json excludes
# commits that touch only Dockerfile.full. It has to. An rta release makes
# rta-plugins' bump-rta.yml release every plugin against it, a plugin release
# makes this workflow move the pins, and if that bump cut a release the two
# repositories would release each other forever. The full image is published
# from docker-full.yml at the version somebody states, and a bump merged
# between releases is picked up by the next run of it.
name: Bump plugins

on:
  schedule:
  - cron: "*/30 * * * *"
  workflow_dispatch:

permissions:
  contents: read

# One run at a time: the scheduled run and a dispatched one landing together
# would each read the open pull request, each decide it is behind, and race
# to push the same branch. The later one waits instead.
concurrency:
  group: bump-plugins
  cancel-in-progress: false

jobs:
  bump:
    name: Open a pull request for any outdated plugin pin
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
    - name: Checks-out repository
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        persist-credentials: false

    - name: Build the line from the official index and what is already there
      id: target
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        set -euo pipefail
        current=$(awk -F'"' '/^ARG PLUGINS="/ { print $2; exit }' Dockerfile.full)
        if [ -z "$current" ]; then
          echo "ARG PLUGINS not found in Dockerfile.full" >&2; exit 1
        fi
        echo "current: $current"

        # One manifest per plugin, one call each; the index is a dozen files
        # and this runs twice an hour. The version is the one the manifest
        # names, which is the tag the plugin released under; the pin resolves
        # through that git tag, never through anything else the manifest
        # claims.
        declare -A want=()
        for name in $(gh api repos/this-is-tobi/rta-plugins/contents/index --jq '.[].name' | sed -n 's/\.yaml$//p'); do
          version=$(gh api "repos/this-is-tobi/rta-plugins/contents/index/${name}.yaml" --jq .content \
            | base64 -d | awk '$1 == "version:" { gsub(/"/, "", $2); print $2; exit }')
          if [ -z "$version" ]; then
            echo "::warning::index/${name}.yaml names no version — left out of this run"; continue
          fi
          want[$name]="v${version}"
        done
        if [ "${#want[@]}" = "0" ]; then
          echo "the official index came back empty; the line is not touched on that evidence" >&2; exit 1
        fi

        declare -A have=()
        for spec in $current; do
          have["${spec%%/*}"]="${spec#*/}"
        done

        nl=$'\n'
        joined=""; bumped=""; kept=""
        new_line=""
        for name in $(printf '%s\n' "${!want[@]}" "${!have[@]}" | sort -u); do
          if [ -n "${want[$name]:-}" ]; then
            tag="${want[$name]}"
            if [ -z "${have[$name]:-}" ]; then
              echo "$name: joins at $tag"
              joined="${joined}- ${name}: joins at \`${tag}\`${nl}"
            elif [ "${have[$name]}" != "$tag" ]; then
              echo "$name: ${have[$name]} -> $tag"
              bumped="${bumped}- ${name}: \`${have[$name]}\` → \`${tag}\`${nl}"
            fi
          else
            tag="${have[$name]}"
            echo "$name: $tag, not in the official index, kept as written"
            kept="${kept}- ${name}: \`${tag}\`, not in the official index, kept as written${nl}"
          fi
          new_line="${new_line}${name}/${tag} "
        done
        new_line="${new_line% }"

        if [ "$new_line" = "$current" ]; then
          echo "the line already matches the official index"; exit 0
        fi
        report=""
        if [ -n "$joined" ]; then
          report="${report}**Joins the image** — a plugin may shell out to a tool; check the package list in Dockerfile.full before merging:${nl}${joined}${nl}"
        fi
        if [ -n "$bumped" ]; then
          report="${report}**Bumped:**${nl}${bumped}${nl}"
        fi
        if [ -n "$kept" ]; then
          report="${report}**Kept, not in the official index:**${nl}${kept}${nl}"
        fi

        # One branch and one pull request, rewritten in place, the way
        # release-please keeps a single release pull request current. A
        # branch per combination was tried first and opened a second pull
        # request the moment rta-plugins released again — which it does
        # within the hour of every rta release, since every plugin follows
        # the SDK — leaving the first one open and already behind. Now a
        # later combination replaces the earlier one on the same branch, and
        # the pull request's body says what it holds today. A run that
        # computes the line the open pull request already carries does
        # nothing.
        branch="bump-plugins"
        open_pr=$(gh pr list --head "$branch" --state open --json number --jq '.[0].number // empty')
        if [ -n "$open_pr" ]; then
          on_pr=$(gh api "repos/${GITHUB_REPOSITORY}/contents/Dockerfile.full?ref=${branch}" --jq .content \
            | base64 -d | awk -F'"' '/^ARG PLUGINS="/ { print $2; exit }')
          if [ "$on_pr" = "$new_line" ]; then
            echo "pull request #$open_pr already carries this line"; exit 0
          fi
          echo "pull request #$open_pr is behind; it will be rewritten"
        fi

        {
          echo "line=$new_line"
          echo "branch=$branch"
          echo "pr=$open_pr"
          echo "report<<BUMP_PLUGINS_REPORT_EOF"
          printf '%s' "$report"
          echo "BUMP_PLUGINS_REPORT_EOF"
        } >> "$GITHUB_OUTPUT"

    - name: Rewrite the pins
      if: steps.target.outputs.line != ''
      run: make bump-plugins PLUGINS="${{ steps.target.outputs.line }}"

    - name: Mint the App token
      if: steps.target.outputs.line != ''
      id: app-token
      uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
      with:
        client-id: ${{ secrets.APP_CLIENT_ID }}
        private-key: ${{ secrets.APP_PRIVATE_KEY }}
        # Contents and pull requests, and nothing about workflows: this
        # workflow never touches a file under .github/workflows/, and an App
        # token cannot without a permission that would apply to every
        # repository the App is installed on.
        permission-contents: write
        permission-pull-requests: write

    - name: Commit, push and open or rewrite the pull request
      if: steps.target.outputs.line != ''
      env:
        GH_TOKEN: ${{ steps.app-token.outputs.token }}
        BRANCH: ${{ steps.target.outputs.branch }}
        PR: ${{ steps.target.outputs.pr }}
        REPORT: ${{ steps.target.outputs.report }}
      run: |
        set -euo pipefail
        git config user.name github-actions[bot]
        git config user.email github-actions[bot]@users.noreply.github.com
        git checkout -b "$BRANCH"
        git add Dockerfile.full
        git commit -m "build(docker): bump the full image's plugin pins" \
          -m "$REPORT"
        # Forced, because the branch is the workflow's own and its previous
        # tip was superseded, never reviewed and kept: the pull request has
        # exactly one commit, from main, every time. Forced with a lease on
        # the tip read just now — an empty tip meaning the branch must not
        # exist yet — so a push nobody here has seen is refused rather than
        # overwritten, and the run fails loudly instead of rewriting it.
        remote="https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
        expected=$(git ls-remote "$remote" "refs/heads/$BRANCH" | cut -f1)
        git push --force-with-lease="refs/heads/$BRANCH:$expected" "$remote" "$BRANCH"
        body="$(printf '%s\n\n%s' \
          "$REPORT" \
          "Opened by the bump workflow, and rewritten by it whenever the official index moves again before this merges. CI is the review: a red check here means the image no longer builds against one of these releases. Nothing is ever removed here; a name that left the index stays on the line until a person takes it off.")"
        if [ -n "$PR" ]; then
          gh pr edit "$PR" --body "$body"
        else
          gh pr create --base main --head "$BRANCH" \
            --title "build(docker): bump the full image's plugin pins" \
            --body "$body"
        fi
