[
  {
    "name": "agent_log",
    "description": "── the text below is written by the plugin, not by rta ──\nThe record of what agents did — one line per call, refusals included\n\nEvery call that arrived over MCP: the capability, the arguments (secrets masked), the profile, what happened, and how it was authorized — no grant needed, a standing grant, or you answering live. The file is chained, so an edited or missing line is visible: --detail verifies it and says where it breaks. This is history and not policy; `rta grant list` is what may happen next.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "after": {
          "description": "only calls after this `seq` — an exact cursor, for shipping the record somewhere",
          "minimum": 0,
          "type": "integer"
        },
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "limit": {
          "default": 30,
          "description": "how many of the most recent calls to show",
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "refused": {
          "description": "only the calls rta would not make",
          "type": "boolean"
        },
        "since": {
          "description": "only calls after this: a duration like `2h`, or a date like 2026-08-30",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "agent_metrics",
    "description": "── the text below is written by the plugin, not by rta ──\nThe record as Prometheus metrics, for a dashboard and an alert\n\nOne command, the standard text exposition format, no listener and no port: `rta agent metrics \u003e /var/lib/node_exporter/textfile_collector/rta.prom` on a timer is the whole integration. Calls by capability, agent, outcome and how they were authorized; grants in force; calls parked waiting for you; and whether the record's hash chain still verifies — which is the one worth an alert, because a record that stops verifying is either a bug or somebody editing it. Nothing is kept: every number is derived from the ledger, so it is a number you could recompute. The Grafana stack's other half needs nothing here — `agent log --after \u003cseq\u003e -o json` is already a cursor over an append-only record, which is what a log shipper wants.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "agent_overview",
    "description": "── the text below is written by the plugin, not by rta ──\nAgent activity at a glance: recent calls, refusals, anything waiting\n\nThe last hour of calls that arrived over MCP, how many were refused, and how many requests are parked waiting for you to answer right now. With --detail: the chain's integrity, where the ledger lives and how big it is.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "agent_pending",
    "description": "── the text below is written by the plugin, not by rta ──\nCalls parked right now, waiting for you to allow or deny\n\nWith `rta mcp serve --consent`, a call that needs a grant nobody issued is parked instead of refused, and waits for you. Each row is one such call: its id, what it wants, against which connection, and how long it will keep waiting. Answer with `rta agent allow \u003cid\u003e` or `rta agent deny \u003cid\u003e`. With --server \u003cname\u003e (a server from remotes.yaml): the same queue read from a remote rta server as a signed operator call, your operator key's passphrase asked first.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "agent_show",
    "description": "── the text below is written by the plugin, not by rta ──\nEverything about one parked call, including what it would do\n\nThe request in full: which capability, which record, against which connection, every argument, and — for a destructive call rta could preview — what running it would actually do, taken from the capability's own --dry-run. That last part is the difference between approving an intention and approving an outcome. Answer with `rta agent allow \u003cid\u003e` or `rta agent deny \u003cid\u003e`.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "the request id from `rta agent pending`",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "audit_agents",
    "description": "── the text below is written by the plugin, not by rta ──\nAudit the AI agents configured on this machine — tools, servers, credentials\n\nAn agent's configuration decides more about a machine's exposure than most of what this plugin grades, and nothing reads it. This does: which tools the model may run, which MCP servers launch with it, whether any of them is handed a credential in plain text, whether one is fetched from a registry at launch rather than pinned, whether the files are readable by anybody else, and whether the endpoint every prompt is sent to is the one you expect. **It reads and never writes** — that file is what grants an agent access to your secrets, and the change is yours to make. Credential *names* are reported and values never are. It also states the boundary out loud: an agent that can run commands can run rta directly, so rta bounds an agent without a shell and is hygiene rather than containment for one with. Refused over MCP, because the subject of this audit is the agent asking. Cites A01:2025 Broken Access Control and A02:2025 Security Misconfiguration. `--fix` prints the exact edit for each finding that has one — a chmod, a pinned version, a scoped shell allowlist, a deny list for rta's own authority-expanding commands — and still writes nothing: the change stays yours to make.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "fix": {
          "description": "print the exact edit for each finding that has one, instead of the grades",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "audit_deps",
    "description": "── the text below is written by the plugin, not by rta ──\nCheck declared dependencies against the OSV vulnerability database\n\nReads what a project already declares — go.mod, any of the four JavaScript lockfiles (npm, pnpm, yarn, bun), uv.lock, poetry.lock, Pipfile.lock, requirements.txt, Cargo.lock, composer.lock, Gemfile.lock, or a CycloneDX/SPDX SBOM sitting beside them — and asks osv.dev once about every pinned dependency, reporting which are named in an advisory. Nothing is resolved, built or crawled: a lockfile is a list a package manager already committed, and reading it is not scanning. Each advisory it finds is then read for its severity and the versions that fix it — a second question asked only about findings, so a project with nothing wrong asks nothing extra — and the records every database publishes for one vulnerability are collapsed onto it, since a package can otherwise be counted twice under a GHSA and a GO identifier for the same CVE. It still points at osv-scanner, trivy or grype for the depth this does not have: whether your code reaches the vulnerable function at all, which no advisory can say. Components whose ecosystem OSV does not recognise are counted and named rather than dropped. --offline inventories without asking anything. --recursive walks a monorepo, skipping node_modules, vendor and build output, which is what a polyglot repository needs: one lockfile per service and no single file that knows about the others. From a terminal --path also takes a repository URL, read shallowly in memory and never written to disk, which is how you audit a repository you have not checked out — refused over MCP, since a URL a caller composes is a request rta makes on its behalf. Cites A03:2025 Software Supply Chain Failures and CWE-1395.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "offline": {
          "description": "inventory the dependencies without querying osv.dev",
          "type": "boolean"
        },
        "path": {
          "default": ".",
          "description": "directory holding the lockfile or SBOM, or the file itself — or, from a terminal, a repository URL (https://, ssh://, git@host:path) read in memory; not over MCP (a path on the machine running rta)",
          "type": "string"
        },
        "recursive": {
          "description": "walk subdirectories too, for a monorepo with a manifest per package",
          "type": "boolean"
        },
        "timeout": {
          "default": 30,
          "description": "query timeout in seconds",
          "maximum": 300,
          "minimum": 1,
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "audit_kube_netpol",
    "description": "── the text below is written by the plugin, not by rta ──\nNamespaces with no NetworkPolicy — unrestricted pod-to-pod traffic\n\nEvery non-system namespace checked for at least one NetworkPolicy. With none, the cluster's default applies: every pod in the namespace can reach every other pod, in either direction. Cites CIS Kubernetes Benchmark 2.0.1 5.3.2.\n\nNarrowed to a namespace it checks that one, including a system namespace if you name it explicitly.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "namespace": {
          "description": "audit one namespace instead of the whole cluster",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "audit_kube_podsecurity",
    "description": "── the text below is written by the plugin, not by rta ──\nPods running privileged, in a host namespace, or with root not excluded\n\nEvery pod cluster-wide, checked against three Pod Security Standards controls: a privileged container (fail), hostNetwork/hostPID/hostIPC (fail), and whether anything in the pod asserts non-root — neither the pod nor any container sets runAsNonRoot or a non-zero runAsUser (warn). The non-root check approximates Kubernetes' own securityContext merge rather than fully simulating it: a pod that asserts non-root at the pod level and never overrides it per container is not flagged, which is the common, correct case; one relying only on an image's own non-root USER with no Kubernetes-level assertion at all is. Cites CWE-250 and Kubernetes Pod Security Standards.\n\nA namespace narrows which pods are read, and the clean result says so — it reports that no pod in that namespace is affected, never that no pod in the cluster is.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "namespace": {
          "description": "audit one namespace instead of the whole cluster",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "audit_kube_quotas",
    "description": "── the text below is written by the plugin, not by rta ──\nNamespaces with no ResourceQuota — unbounded resource consumption\n\nEvery non-system namespace checked for at least one ResourceQuota. CIS Kubernetes Benchmark has no numbered control for this — confirmed absent rather than assumed — so this cites NSA/CISA Kubernetes Hardening Guidance's \"Resource policies\" section instead.\n\nNarrowed to a namespace it checks that one, including a system namespace if you name it explicitly — the system-namespace filter exists so a cluster-wide sweep stays quiet, not to refuse a direct question.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "namespace": {
          "description": "audit one namespace instead of the whole cluster",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "audit_kube_rbac",
    "description": "── the text below is written by the plugin, not by rta ──\nOverly-broad RBAC: cluster-admin bindings and wildcard rules\n\nEvery ClusterRoleBinding to cluster-admin, and every Role/ClusterRole rule using a wildcard verb, resource or apiGroup. cluster-admin bound to a broad group (system:authenticated, system:unauthenticated, system:masters) grades fail; bound to anything else grades warn — sometimes a real operator's binding, always worth reading. Cites CIS Kubernetes Benchmark 2.0.1 5.1.1 and 5.1.3.\n\nNarrowed to a namespace it checks that namespace's Roles only. ClusterRoleBindings and ClusterRoles belong to no namespace, so they are not examined at all — reported in the result rather than left implied, since a scoped run that silently skipped the cluster-admin check would read as a clean RBAC posture.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "namespace": {
          "description": "audit one namespace instead of the whole cluster",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "audit_mail",
    "description": "── the text below is written by the plugin, not by rta ──\nAudit a domain's email authentication: SPF, DKIM, DMARC, MTA-STS\n\nAnswers \"can somebody send mail as this domain\" from the records the domain publishes: SPF (present, singular, how it ends, and how close it is to RFC 7208's ten-lookup limit past which it silently stops applying), DMARC (policy, rollout percentage, whether anything reports back), DKIM for a given selector, MTA-STS and TLS-RPT for the server-to-server hop, and MX — including RFC 7505's null MX, which is a hardening measure rather than an omission. Every finding cites an OWASP Top 10:2025 category and a MITRE CWE. With --detail: one section per area plus the cited references with lookup URLs. Read-only, a handful of lookups of names derived from the domain by rule — nothing is enumerated, which is why DKIM needs its selector handed to it.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "domain": {
          "description": "domain to audit",
          "type": "string"
        },
        "selector": {
          "description": "DKIM selector to check (the s= tag of a DKIM-Signature)",
          "type": "string"
        },
        "timeout": {
          "default": 15,
          "description": "lookup timeout in seconds",
          "maximum": 300,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "domain"
      ],
      "type": "object"
    }
  },
  {
    "name": "audit_web",
    "description": "── the text below is written by the plugin, not by rta ──\nAudit a web host: TLS, certificate, headers, cookies, exposure\n\nOne HTTPS request, graded: TLS version and cipher, certificate validity, expiry and signature algorithm, the security headers a host should send (HSTS, CSP — checked for weak directives like unsafe-inline, not just presence — X-Frame-Options/frame-ancestors, cross-origin isolation headers), CORS behavior (probed with an Origin no real browser would send, to catch blind reflection), cookie flags, and the version/stack details a host should not leak. Every finding cites an OWASP Top 10:2025 category and a MITRE CWE, so a result traces back to a named control rather than an opinion. With --detail: one section per area plus the cited references with lookup URLs. Read-only — it only inspects the response the host volunteers, plus the one Origin header the CORS probe adds.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"host\"). You cannot issue one yourself — ask the operator to run `rta grant allow audit.web`.",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "host": {
          "description": "host or URL to audit",
          "type": "string"
        },
        "timeout": {
          "default": 15,
          "description": "request timeout in seconds",
          "maximum": 300,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "host"
      ],
      "type": "object"
    }
  },
  {
    "name": "audit_why",
    "description": "── the text below is written by the plugin, not by rta ──\nTrace one dependency back to what pulled it in\n\nAnswers the question an advisory raises second: did we ask for this, or did something else pull it in — and if something else, what. Reads the same manifests `audit deps` does and draws the package at the root with everything that requires it beneath, up to the dependencies the project asks for by name. Nothing is resolved, installed or fetched. Formats differ in what they record: go.mod marks a require direct or indirect and stores no edges, while the four JavaScript lockfiles, Cargo, uv, Poetry, composer, Gemfile.lock, pip-compile's `# via` annotations and a CycloneDX SBOM all carry the graph. Where the file does not say, this says so and hands over the package manager's own command — `go mod why`, `pnpm why`, `cargo tree --invert` — with the package already in it.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "package": {
          "description": "the package to trace, as the lockfile spells it",
          "type": "string"
        },
        "path": {
          "default": ".",
          "description": "directory holding the lockfile or SBOM, or the file itself — or, from a terminal, a repository URL (https://, ssh://, git@host:path) read in memory; not over MCP (a path on the machine running rta)",
          "type": "string"
        },
        "recursive": {
          "description": "walk subdirectories too, for a monorepo with a manifest per package",
          "type": "boolean"
        }
      },
      "required": [
        "package"
      ],
      "type": "object"
    }
  },
  {
    "name": "cert_chain",
    "description": "── the text below is written by the plugin, not by rta ──\nShow the certificate chain presented by a host\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "target": {
          "description": "host[:port] to connect to, or a path to a PEM file (a path on the machine running rta)",
          "type": "string"
        },
        "timeout": {
          "default": 10,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "target"
      ],
      "type": "object"
    }
  },
  {
    "name": "cert_expiry",
    "description": "── the text below is written by the plugin, not by rta ──\nCheck certificate expiry for one or more hosts\n\nSame reasoning as `net probe`: the hosts are the caller's choice, and what a certificate says about itself — subject, issuer, DNS names — is read as tool output the same way a banner is. Needs a grant, one per host listed.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"targets\"). You cannot issue one yourself — ask the operator to run `rta grant allow cert.expiry`.",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "targets": {
          "description": "hosts to check (host[:port])",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "default": 10,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        },
        "warn-days": {
          "default": 30,
          "description": "flag certificates expiring within this many days",
          "type": "integer"
        }
      },
      "required": [
        "targets"
      ],
      "type": "object"
    }
  },
  {
    "name": "cert_inspect",
    "description": "── the text below is written by the plugin, not by rta ──\nShow certificate details for a host or PEM file\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "target": {
          "description": "host[:port] to connect to, or a path to a PEM file (a path on the machine running rta)",
          "type": "string"
        },
        "timeout": {
          "default": 10,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "target"
      ],
      "type": "object"
    }
  },
  {
    "name": "cert_pem",
    "description": "── the text below is written by the plugin, not by rta ──\nPrint the certificate chain as PEM\n\nThe bytes, not a description of them. `cert chain` draws what a host presents so a person can read it; this hands the same certificates back in the form every other tool takes one — a Kubernetes ConfigMap, a Dockerfile COPY, `update-ca-certificates`, a paste into somebody's terminal.\n\n--include issuers is the one to reach for behind a private CA: it drops the leaf and leaves the chain that has to be *trusted*, which is what a ca-bundle is. chain (the default) is everything the host presented, leaf is the end-entity certificate alone.\n\nA presented chain is what the host chose to send and is not always complete — a server that omits its intermediate presents a leaf that validates nowhere else, and this reports what arrived rather than filling the gap from a trust store, because a bundle that silently differs from what the server serves is how a working local test hides a broken deployment.\n\nRead, and it stays read from anywhere but a terminal: --out names a path on *this* machine, so it is a person's flag only and an MCP caller always gets the PEM back in the response.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "include": {
          "default": "chain",
          "description": "which certificates to print",
          "enum": [
            "chain",
            "issuers",
            "leaf"
          ],
          "type": "string"
        },
        "target": {
          "description": "host[:port] to connect to, or a path to a PEM file (a path on the machine running rta)",
          "type": "string"
        },
        "timeout": {
          "default": 10,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "target"
      ],
      "type": "object"
    }
  },
  {
    "name": "cert_tls",
    "description": "── the text below is written by the plugin, not by rta ──\nReport the negotiated TLS version and cipher for a host\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "target": {
          "description": "host[:port] to connect to, or a path to a PEM file (a path on the machine running rta)",
          "type": "string"
        },
        "timeout": {
          "default": 10,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "target"
      ],
      "type": "object"
    }
  },
  {
    "name": "codec_b64",
    "description": "── the text below is written by the plugin, not by rta ──\nBase64 encode or decode a value\n\nDecoding accepts standard, URL-safe, and unpadded variants without being told which — the caller already has the encoded value, so being forgiving about which base64 dialect produced it costs nothing.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "decode": {
          "description": "decode instead of encode",
          "type": "boolean"
        },
        "url": {
          "description": "use the URL-safe alphabet when encoding",
          "type": "boolean"
        },
        "value": {
          "description": "the text to transform",
          "type": "string"
        }
      },
      "required": [
        "value"
      ],
      "type": "object"
    }
  },
  {
    "name": "codec_hex",
    "description": "── the text below is written by the plugin, not by rta ──\nHex encode or decode a value\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "decode": {
          "description": "decode instead of encode",
          "type": "boolean"
        },
        "value": {
          "description": "the text to transform",
          "type": "string"
        }
      },
      "required": [
        "value"
      ],
      "type": "object"
    }
  },
  {
    "name": "codec_jwt",
    "description": "── the text below is written by the plugin, not by rta ──\nDecode a JWT's header and claims for inspection\n\nUnverified by default and clearly labeled as such: this is for reading a token while debugging, not for authenticating one. No signature check is performed — anyone can hand you a token with any claims at all.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "token": {
          "description": "the JWT to decode",
          "type": "string"
        }
      },
      "required": [
        "token"
      ],
      "type": "object"
    }
  },
  {
    "name": "codec_url",
    "description": "── the text below is written by the plugin, not by rta ──\nURL-escape or unescape a value\n\nQuery-component escaping (spaces become +), the form almost everyone means by \"URL encode this\" — the value for a query string or form body.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "decode": {
          "description": "decode instead of encode",
          "type": "boolean"
        },
        "value": {
          "description": "the text to transform",
          "type": "string"
        }
      },
      "required": [
        "value"
      ],
      "type": "object"
    }
  },
  {
    "name": "debug_ansi",
    "description": "── the text below is written by the plugin, not by rta ──\nBreak down the ANSI escape sequences in a string, byte by byte\n\nWalks the input the way a terminal would, one printable run or control/escape sequence at a time, and names what each one does — SGR colors and attributes, cursor movement, screen/line erase, and the OSC sequences most worth knowing about on sight: window title, hyperlink target, and the system clipboard write (decoded, not left as base64). Never prints a raw control byte back at the terminal it is running in — the whole point is seeing what a sequence does without it happening.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "input": {
          "description": "text containing escape sequences; reads piped stdin when omitted",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "fs_hash",
    "description": "── the text below is written by the plugin, not by rta ──\nChecksum a file, and compare it against an expected value\n\nHashes a file and, given --expect, says plainly whether it matches — which is the actual task, and the one comparing two hex strings by eye is bad at. The comparison is case-insensitive and tolerates the \"sha256:\" prefix and the surrounding whitespace that come with a pasted checksum. It is not a signature check: it says a file is the one somebody described, not that the description came from anybody in particular.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "algo": {
          "default": "sha256",
          "description": "hash algorithm",
          "enum": [
            "sha256",
            "sha512",
            "sha1",
            "md5"
          ],
          "type": "string"
        },
        "expect": {
          "description": "checksum to compare against",
          "type": "string"
        },
        "path": {
          "description": "file to hash (a path on the machine running rta)",
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    }
  },
  {
    "name": "fs_tree",
    "description": "── the text below is written by the plugin, not by rta ──\nShow a directory as a tree, with sizes\n\nThe shape of a directory, a few levels at a time, with each entry's size beside it. Entries are ordered directories first and then by name, the order a person reads a listing in. Truncated branches say how many entries they are hiding rather than trailing off, so the tree never claims a directory is smaller than it is. With --detail: what the walk covered, the tree, and everything it left out — depth, per-directory limit, hidden entries, mount points and unreadable directories gathered in one place instead of scattered through the branches they happened in.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "all": {
          "description": "include hidden entries",
          "type": "boolean"
        },
        "depth": {
          "default": 2,
          "description": "how many levels to show",
          "maximum": 12,
          "minimum": 1,
          "type": "integer"
        },
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "limit": {
          "default": 12,
          "description": "entries to show per directory",
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "path": {
          "default": ".",
          "description": "directory to show (a path on the machine running rta)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "fs_usage",
    "description": "── the text below is written by the plugin, not by rta ──\nShow what is using space under a path, biggest first\n\nTotals every entry under a path and ranks them, which is the question `du -sh * | sort -h | tail` is always asked to answer. Directories are summed recursively; the share column is of the scanned total, not of the disk, so it adds up to what you are looking at. Hidden entries are included — they are usually the answer. With --detail: the ranking, the largest individual files found anywhere beneath, and what was skipped. Follows no symlinks and crosses no filesystem boundary, so a scan cannot loop or wander onto a network mount.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "depth": {
          "default": 0,
          "description": "how deep to descend when totalling (0 = no limit)",
          "type": "integer"
        },
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "limit": {
          "default": 20,
          "description": "how many entries to rank",
          "maximum": 1000,
          "minimum": 1,
          "type": "integer"
        },
        "path": {
          "default": ".",
          "description": "directory to measure (a path on the machine running rta)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "gen_overview",
    "description": "── the text below is written by the plugin, not by rta ──\nA sampler of the shapes worth generating, ready to use\n\nGenerates one of each common shape side by side — passwords, encryption key material, URL-safe and TOTP tokens, UUIDs — labelled with what it is for, the entropy it actually carries and the command that reproduces it, so picking the right shape does not require remembering the flags first. With --detail: every preset, plus why a \"32-character key\" is not a 32-byte one. Every value is real and freshly generated; nothing is an example.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "gen_password",
    "description": "── the text below is written by the plugin, not by rta ──\nGenerate a cryptographically random password\n\ncrypto/rand only, with unbiased alphabet selection (rand.Int, never rand.Intn or modulo, which biases low indices unless the alphabet size divides the RNG range evenly). Reports entropy in bits: length x log2(alphabet size).\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "count": {
          "default": 1,
          "description": "how many to generate",
          "type": "integer"
        },
        "exclude-ambiguous": {
          "description": "drop look-alike characters: 0O1lI",
          "type": "boolean"
        },
        "length": {
          "default": 20,
          "description": "characters",
          "maximum": 4096,
          "minimum": 1,
          "type": "integer"
        },
        "no-digits": {
          "description": "exclude digits",
          "type": "boolean"
        },
        "no-lower": {
          "description": "exclude lowercase letters",
          "type": "boolean"
        },
        "no-upper": {
          "description": "exclude uppercase letters",
          "type": "boolean"
        },
        "symbols": {
          "description": "include symbols: !@#$%^\u0026*()-_=+[]{}\u003c\u003e?",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "gen_token",
    "description": "── the text below is written by the plugin, not by rta ──\nGenerate random bytes, formatted as hex/base64/base32\n\ncrypto/rand bytes, formatted with --encoding. base32 at the right length is already a TOTP secret (RFC 4648/6238) — there is no separate gen.totp.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "encoding": {
          "default": "hex",
          "description": "output encoding",
          "enum": [
            "hex",
            "base64",
            "base64url",
            "base32"
          ],
          "type": "string"
        },
        "length": {
          "default": 32,
          "description": "bytes of entropy",
          "maximum": 4096,
          "minimum": 1,
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "gen_uuid",
    "description": "── the text below is written by the plugin, not by rta ──\nGenerate a UUID: v4 random (default) or v7 time-ordered\n\nv1/v3/v5/v6 are deliberately not offered: v1 leaks the generating machine's MAC address and a timestamp, v3/v5 hash a namespace+name instead of generating anything (a different operation), and v6 is superseded by v7 for the same time-ordering goal, per RFC 9562's own recommendation.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "count": {
          "default": 1,
          "description": "how many to generate",
          "type": "integer"
        },
        "version": {
          "default": "4",
          "description": "UUID version",
          "enum": [
            "4",
            "7"
          ],
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "git_blame",
    "description": "── the text below is written by the plugin, not by rta ──\nWho last touched each line of a file, and when\n\nOne row per line: who last changed it, when, and the commit that did — the structured equivalent of `git blame`. Reads the whole file's history to answer, so it is not offered as a dashboard tile the way a bounded, no-input capability would be; a large file with a long history can take a real moment.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "file": {
          "description": "path to the file, relative to the repository root (a path on the machine running rta)",
          "type": "string"
        },
        "path": {
          "default": ".",
          "description": "repository path, or a subdirectory of one — or, from a terminal, a remote URL (https://, ssh://, git@host:path) cloned in memory; not over MCP (a path on the machine running rta)",
          "type": "string"
        }
      },
      "required": [
        "file"
      ],
      "type": "object"
    }
  },
  {
    "name": "git_branches",
    "description": "── the text below is written by the plugin, not by rta ──\nLocal branches, and which one is checked out\n\nEvery local branch, alphabetically, with the checked-out one marked. A detached HEAD — checked out at a commit rather than a branch — is reported as its own row rather than left for the caller to notice no branch was marked.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "default": ".",
          "description": "repository path, or a subdirectory of one — or, from a terminal, a remote URL (https://, ssh://, git@host:path) cloned in memory; not over MCP (a path on the machine running rta)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "git_config",
    "description": "── the text below is written by the plugin, not by rta ──\nEffective config across system, global and local scope\n\nEvery key set in system, global or local config, one row per scope it's set in — the same three files `git config --list --show-origin` reads, before any of them override each other: local wins over global, global wins over system. A key missing from a scope simply has no row there rather than one with an empty value. `[include]`/`[includeIf]` directives are shown as written, not followed into the file they point at. Over MCP only the repository's own config is returned: the machine-wide scopes are the operator's, not the repository's. Values that carry a credential are masked on every surface.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "default": ".",
          "description": "repository path, or a subdirectory of one — or, from a terminal, a remote URL (https://, ssh://, git@host:path) cloned in memory; not over MCP (a path on the machine running rta)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "git_diff",
    "description": "── the text below is written by the plugin, not by rta ──\nLine-level changes — uncommitted, or one commit against its parent\n\nUnified diff text, the structured-plugin equivalent of `git diff`. With no --commit, this is every uncommitted change — staged and unstaged together — against HEAD; git.status already answers which paths changed, this answers what changed in them. --commit diffs that one commit against its own parent instead, the equivalent of `git show \u003ccommit\u003e`'s patch half. Diffing two arbitrary commits against each other is deliberately not offered in this first cut — the two cases above cover what an agent inspecting a repository's current state actually needs, and a revision-range comparison is a distinct enough question to design on its own rather than bolt on.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "commit": {
          "description": "diff this commit against its own parent, instead of the working tree against HEAD",
          "type": "string"
        },
        "path": {
          "default": ".",
          "description": "repository path, or a subdirectory of one — or, from a terminal, a remote URL (https://, ssh://, git@host:path) cloned in memory; not over MCP (a path on the machine running rta)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "git_hooks",
    "description": "── the text below is written by the plugin, not by rta ──\nWhat's in the hooks directory, and which of it git would actually run\n\nEvery entry in the repository's hooks directory, judged by the same rule git itself uses to decide whether one fires on commit, push and the rest: named exactly (a `.sample` suffix never runs) and executable. A hook is an arbitrary script that runs on this machine, so this reports what would actually execute, not merely what a directory listing shows.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "default": ".",
          "description": "repository path, or a subdirectory of one — or, from a terminal, a remote URL (https://, ssh://, git@host:path) cloned in memory; not over MCP (a path on the machine running rta)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "git_log",
    "description": "── the text below is written by the plugin, not by rta ──\nRecent commit history\n\nThe most recent commits reaching HEAD, newest first — hash, author, date and the message's own first line, each a table row rather than text to parse. --file narrows it to commits that touched one path, the structured equivalent of `git log -- \u003cpath\u003e`.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "file": {
          "description": "limit to commits that touched this path (a path on the machine running rta)",
          "type": "string"
        },
        "limit": {
          "default": 20,
          "description": "how many of the most recent commits to show",
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "path": {
          "default": ".",
          "description": "repository path, or a subdirectory of one — or, from a terminal, a remote URL (https://, ssh://, git@host:path) cloned in memory; not over MCP (a path on the machine running rta)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "git_overview",
    "description": "── the text below is written by the plugin, not by rta ──\nBranch, working-tree state and the latest commit, at a glance\n\nA one-line answer to \"what state is this repository in\": which branch is checked out (or that HEAD is detached), whether the working tree is clean, and the latest commit. With --detail (and on any full-page surface) it expands into the same status, log and branches views their own capabilities return — one shape, not a second implementation of each.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "path": {
          "default": ".",
          "description": "repository path, or a subdirectory of one — or, from a terminal, a remote URL (https://, ssh://, git@host:path) cloned in memory; not over MCP (a path on the machine running rta)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "git_remotes",
    "description": "── the text below is written by the plugin, not by rta ──\nWhere this repository fetches from and pushes to\n\nThe other half of \"which branch am I on\": which server that branch reaches, and whether this machine has ever heard from it. Three remotes with confusingly similar URLs is how somebody pushes a fix to their fork and waits for a review nobody can see.\n\nBranches counts what this repository knows about that remote — the refs a fetch left behind — so a remote that has never been fetched reads as 0 rather than as missing.\n\nA credential embedded in a remote URL is masked, the same rule `git config` follows: `https://user:token@host/repo.git` is a password in a file people paste into issues, and it is not what anybody is asking this for.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "default": ".",
          "description": "repository path, or a subdirectory of one — or, from a terminal, a remote URL (https://, ssh://, git@host:path) cloned in memory; not over MCP (a path on the machine running rta)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "git_status",
    "description": "── the text below is written by the plugin, not by rta ──\nWhat has changed in the working tree, staged and unstaged\n\nThe structured equivalent of `git status --porcelain`: every path with a staged change, an unstaged change, or neither yet — added, tracked at all — one row per path, both halves shown side by side rather than requiring the two-column code to be decoded by eye.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "default": ".",
          "description": "repository path, or a subdirectory of one — or, from a terminal, a remote URL (https://, ssh://, git@host:path) cloned in memory; not over MCP (a path on the machine running rta)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "grant_guard_status",
    "description": "── the text below is written by the plugin, not by rta ──\nWhether grant issuance requires the passphrase\n\nOn or off, since when, the verification key's fingerprint, and how many grants are held under it. Refused over MCP like every grant surface: whether the guard stands is part of the map of what an agent could reach.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "grant_list",
    "description": "── the text below is written by the plugin, not by rta ──\nList what AI agents are currently allowed to do\n\nReadable without unlocking anything, so the question stays answerable in a hurry. Expired grants are dropped on read. With --detail: what is currently allowed, then everything an agent can reach with no grant at all, and everything that would need one — because \"what did I allow\" is only half of \"what can it do\". With --server \u003cname\u003e (a server from remotes.yaml): the same roster read from a remote rta server as a signed operator call, your operator key's passphrase asked first. Can only be run by a person at a terminal, the same as grant.allow/renew/revoke: the roster names every agent by name, which is exactly the cross-agent visibility an agent asking about itself must not get.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "http_get",
    "description": "── the text below is written by the plugin, not by rta ──\nGET a URL and show status, timing and body\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"url\"). You cannot issue one yourself — ask the operator to run `rta grant allow http.get`.",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "basic": {
          "description": "basic auth as user:password",
          "type": "string"
        },
        "bearer": {
          "description": "bearer token (Authorization: Bearer ...)",
          "type": "string"
        },
        "header": {
          "description": "request header, repeatable: -H 'Key: Value'",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "default": 30,
          "description": "request timeout in seconds",
          "maximum": 600,
          "minimum": 1,
          "type": "integer"
        },
        "url": {
          "description": "request URL",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    }
  },
  {
    "name": "http_head",
    "description": "── the text below is written by the plugin, not by rta ──\nHEAD a URL and show status, timing and headers\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"url\"). You cannot issue one yourself — ask the operator to run `rta grant allow http.head`.",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "basic": {
          "description": "basic auth as user:password",
          "type": "string"
        },
        "bearer": {
          "description": "bearer token (Authorization: Bearer ...)",
          "type": "string"
        },
        "header": {
          "description": "request header, repeatable: -H 'Key: Value'",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "default": 30,
          "description": "request timeout in seconds",
          "maximum": 600,
          "minimum": 1,
          "type": "integer"
        },
        "url": {
          "description": "request URL",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    }
  },
  {
    "name": "keys_list",
    "description": "── the text below is written by the plugin, not by rta ──\nList the SSH private keys in ~/.ssh, how protected each is, and whether it can be backed up\n\nFinds keys by their PEM preamble rather than by an id_* name, so a key called work_ed25519 — or symlinked in from a dotfiles repository under any name at all — is listed, and a stray file that was never a key is not. Reads only public data: a key's .pub sibling for its type, fingerprint and comment, and whether the private key itself is passphrase-protected — the same check `rta doctor` uses, which parses far enough to see a key is locked without ever supplying a passphrase. Never decrypts anything. Backup-eligible means ed25519: `keys.backup` has nothing to encode as words for an RSA or ECDSA key, which carries no single seed. An Exposed column appears only when some key's permissions let another account on this machine read it, which is both a credential exposure and the reason ssh has stopped accepting that key.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "kv_list",
    "description": "── the text below is written by the plugin, not by rta ──\nList stored keys (names and metadata only, never values)\n\nNever returns a value or a preview of one — only key names, what kind of thing each is, its size, its description and when it changed. With --detail: the source filename of anything stored from disk.\n\n--match is the \"which one was it called?\" filter: a substring of the name or the description, case-insensitive, so `kv list --match aws` finds `prod-deploy-key` when the description is the only place the word AWS appears.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "kind": {
          "description": "only entries of this kind",
          "enum": [
            "string",
            "json",
            "certificate",
            "private-key",
            "public-key",
            "ssh-key",
            "file"
          ],
          "type": "string"
        },
        "match": {
          "description": "only keys whose name or description contains this (case-insensitive)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "kv_recipients",
    "description": "── the text below is written by the plugin, not by rta ──\nList the public keys that can decrypt the store\n\nAnswers \"who can read this?\" without unlocking anything — recipients are public keys, so the list is plaintext by design. An empty list means the store is passphrase-encrypted.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "kv_show",
    "description": "── the text below is written by the plugin, not by rta ──\nShow everything about one entry except its value\n\nThe detail page for a stored key: what kind of thing it is, what it is for, how big it is, where it came from and when it changed. Deliberately not the value — that is `kv get` (prints it) or `kv copy` (does not), both writes for exactly that reason. This stays Read because everything on it is metadata you can safely put on a screen.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "key": {
          "description": "key to describe",
          "type": "string"
        }
      },
      "required": [
        "key"
      ],
      "type": "object"
    }
  },
  {
    "name": "kv_status",
    "description": "── the text below is written by the plugin, not by rta ──\nWhere the store is and what can open it\n\nEverything about the store that can be known without unlocking it: whether it exists, how big it is, when it last changed, whether it is locked with a passphrase or to keys, and whether a key is available in this environment. With --detail it also lists who can open the store and what is in it — names, kinds and sizes, never a value or a preview of one — when a key is already at hand; when none is, it says so instead of asking, so the compact answer never turns into a passphrase prompt nobody expected.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "lock_list",
    "description": "── the text below is written by the plugin, not by rta ──\nWho is frozen right now, and why\n\nThe live locks: kind, principal, the note the locked party is shown, who placed it and until when. With --server \u003cname\u003e: reads a remote server's locks as a signed operator call. Never reachable over MCP — who an operator has frozen is incident state, and not an agent's to enumerate.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "net_dns",
    "description": "── the text below is written by the plugin, not by rta ──\nResolve DNS records for a name, or a name for an address\n\n--type auto (the default) asks the question you usually mean: A, AAAA and CNAME for a name, PTR for an address. --server sends the query to a specific resolver instead of the system one, which is how you tell \"the record is wrong\" apart from \"my resolver is stale\". With --detail: the query, the resolver that answered, and how long it took.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "name": {
          "description": "name or IP address to resolve",
          "type": "string"
        },
        "server": {
          "description": "resolver to query, e.g. 1.1.1.1 or 9.9.9.9:53 (default: the system resolver)",
          "type": "string"
        },
        "timeout": {
          "default": 5,
          "description": "query timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        },
        "type": {
          "default": "auto",
          "description": "which record type to ask for",
          "enum": [
            "auto",
            "A",
            "AAAA",
            "CNAME",
            "MX",
            "NS",
            "TXT",
            "PTR",
            "SRV"
          ],
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "name": "net_hosts_list",
    "description": "── the text below is written by the plugin, not by rta ──\nList hosts-file entries, including disabled ones\n\nOne row per hostname, since a hostname is what you look up, disable and remove. Entries commented out by `net hosts toggle` are shown as disabled rather than hidden as comments.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "file": {
          "description": "operate on this file instead of the system one (a container's, a chroot's) (a path on the machine running rta)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "net_info",
    "description": "── the text below is written by the plugin, not by rta ──\nLocal network overview: interfaces, DNS, proxy, throughput\n\nReports local facts only — it never calls external services, so no public-IP lookup. Proxy credentials are masked. Throughput is sampled over 500ms. With --detail: every interface with MAC, MTU, flags and addresses, per-interface traffic counters, the full resolver list and proxy environment.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "net_listen",
    "description": "── the text below is written by the plugin, not by rta ──\nWhat this machine is listening on, and which process owns it\n\nEvery TCP and UDP socket this machine has open for business, with the address it is bound to, how far that reaches (loopback, one address, or all interfaces) and the process behind it.\n\nTwo honest limits. **UDP has no listening state** — a bound UDP socket is in no state at all — so a socket with a port and no peer is shown whether it is a server waiting or a resolver with a query in flight; the kernel does not tell them apart and neither does netstat. And **the answer is only as complete as the caller's privileges**, differently on each platform: Linux reads the whole socket table from /proc but can only name the process behind sockets it may look into, so the Process column empties; macOS reads through lsof, which shows only what the caller may see, so the row is absent entirely. Neither platform silently substitutes one for the other.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "port": {
          "description": "only this port — the \"who has 8080\" question",
          "maximum": 65535,
          "minimum": 1,
          "type": "integer"
        },
        "proto": {
          "description": "only this protocol (tcp covers tcp6, udp covers udp6)",
          "enum": [
            "tcp",
            "udp"
          ],
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "net_ping",
    "description": "── the text below is written by the plugin, not by rta ──\nPing a host and report latency statistics\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "count": {
          "default": 4,
          "description": "number of probes",
          "maximum": 100,
          "minimum": 1,
          "type": "integer"
        },
        "graph": {
          "description": "plot per-probe latency instead of summary stats",
          "type": "boolean"
        },
        "host": {
          "description": "host to ping",
          "type": "string"
        },
        "timeout": {
          "default": 10,
          "description": "overall timeout in seconds",
          "maximum": 300,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "host"
      ],
      "type": "object"
    }
  },
  {
    "name": "net_port",
    "description": "── the text below is written by the plugin, not by rta ──\nTCP connect-scan ports on a host\n\nWhich ports answer, not what they say — `net probe` shows the banner for one; this maps many at once. Needs a grant for the same reason: the host is the caller's choice.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"host\"). You cannot issue one yourself — ask the operator to run `rta grant allow net.port`.",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "host": {
          "description": "host to scan",
          "type": "string"
        },
        "ports": {
          "default": "22,80,443",
          "description": "comma-separated ports and ranges, e.g. 22,80,8000-8010 (up to 4096 at a time)",
          "type": "string"
        },
        "timeout": {
          "default": 2,
          "description": "per-port timeout in seconds",
          "maximum": 60,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "host"
      ],
      "type": "object"
    }
  },
  {
    "name": "net_probe",
    "description": "── the text below is written by the plugin, not by rta ──\nOpen a TCP connection and report what answers\n\nWhat telnet \u003chost\u003e \u003cport\u003e is really used for: prove the port accepts a connection, time the handshake, and show the banner the service volunteers. --tls completes a TLS handshake first and reports the negotiated version and cipher. It only ever listens; to speak first — the protocols that expect the client to open — use `net send`, which is a write. Both need a grant: the destination is the caller's choice either way, and whatever it says back is read as tool output.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"host\"). You cannot issue one yourself — ask the operator to run `rta grant allow net.probe`.",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "host": {
          "description": "host to connect to",
          "type": "string"
        },
        "port": {
          "description": "port to connect to",
          "maximum": 65535,
          "minimum": 1,
          "type": "integer"
        },
        "timeout": {
          "default": 5,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        },
        "tls": {
          "description": "complete a TLS handshake after connecting",
          "type": "boolean"
        },
        "wait": {
          "default": 2,
          "description": "seconds to wait for the service to say something",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "host",
        "port"
      ],
      "type": "object"
    }
  },
  {
    "name": "net_resolver_list",
    "description": "── the text below is written by the plugin, not by rta ──\nShow the system resolver, and who controls it\n\nresolv.conf is the file people most often edit to no effect: on most modern systems it is generated and replaced. This leads with who owns it, because that decides whether anything else in it is worth changing.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "file": {
          "description": "operate on this file instead of the system one (a container's, a chroot's) (a path on the machine running rta)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "net_trace",
    "description": "── the text below is written by the plugin, not by rta ──\nTrace the route to a host, hop by hop\n\nICMP echo probes with a rising TTL: each hop that expires one reports itself. Answers the question ping cannot — not just \"is it reachable\" but \"where does it stop\". Silent hops show as *; that is a router declining to reply, not necessarily a fault.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "host": {
          "description": "host to trace",
          "type": "string"
        },
        "max-hops": {
          "default": 30,
          "description": "stop after this many hops",
          "maximum": 64,
          "minimum": 1,
          "type": "integer"
        },
        "probes": {
          "default": 3,
          "description": "probes per hop",
          "maximum": 10,
          "minimum": 1,
          "type": "integer"
        },
        "resolve": {
          "default": true,
          "description": "reverse-resolve hop addresses to names",
          "type": "boolean"
        },
        "timeout": {
          "default": 2,
          "description": "per-probe timeout in seconds",
          "maximum": 60,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "host"
      ],
      "type": "object"
    }
  },
  {
    "name": "note_list",
    "description": "── the text below is written by the plugin, not by rta ──\nList notes\n\nWith --detail: adds creation date and a preview of each note's body.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "tag": {
          "description": "only notes with any of these tags",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "note_search",
    "description": "── the text below is written by the plugin, not by rta ──\nSearch notes by title, body or tag\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "query": {
          "description": "search terms",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "name": "note_show",
    "description": "── the text below is written by the plugin, not by rta ──\nShow one note: metadata, content, references\n\nMetadata (tags, size, age) stays separate from the markdown content, which is rendered richly on human surfaces. Cross-references (\"#12\" in a body) resolve both ways.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "note id",
          "type": "integer"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "note_tags",
    "description": "── the text below is written by the plugin, not by rta ──\nList tags in use and how many notes carry each\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "operator_status",
    "description": "── the text below is written by the plugin, not by rta ──\nYour operator key at a glance — or a remote server's, with --server\n\nWithout --server: whether this machine holds an operator key, its fingerprint, and the exact line to paste into a server's --operators roster. With --server \u003cname\u003e (from remotes.yaml, beside your config): asks that server who it is — version, agent name, guard state, enrolled operators — as a signed call, so it also proves your enrollment end to end.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "default": "operator",
          "description": "the label to print in the roster line",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "sys_cpu",
    "description": "── the text below is written by the plugin, not by rta ──\nShow CPU model, core count and current usage\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "cores": {
          "description": "per-core usage as a bar chart",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "sys_disk",
    "description": "── the text below is written by the plugin, not by rta ──\nShow disk usage per mounted filesystem\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "all": {
          "description": "include pseudo, duplicate and zero-size filesystems",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "sys_host",
    "description": "── the text below is written by the plugin, not by rta ──\nShow host info: OS, kernel, uptime, addresses\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "sys_load",
    "description": "── the text below is written by the plugin, not by rta ──\nShow load averages, normalized per core\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "sys_mem",
    "description": "── the text below is written by the plugin, not by rta ──\nShow memory and swap usage\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "sys_overview",
    "description": "── the text below is written by the plugin, not by rta ──\nOne-screen host health: cpu, memory, load, disk, uptime\n\nA dense one-line-per-subsystem summary. With --detail (and on any full-page surface) it expands into a full report: identity, per-core usage, memory and swap breakdown, every filesystem, sensors and top processes. Lines a platform cannot provide are omitted rather than failing the view.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "sys_ps",
    "description": "── the text below is written by the plugin, not by rta ──\nList top processes by CPU or memory\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "limit": {
          "default": 15,
          "description": "maximum processes to list",
          "maximum": 1000,
          "minimum": 1,
          "type": "integer"
        },
        "sort": {
          "default": "cpu",
          "description": "sort by",
          "enum": [
            "cpu",
            "mem"
          ],
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "sys_temp",
    "description": "── the text below is written by the plugin, not by rta ──\nShow sensor temperatures\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "time_at",
    "description": "── the text below is written by the plugin, not by rta ──\nConvert an instant between epoch, UTC, local time and a named zone\n\nTakes an instant in whatever form you have it — an epoch number at any precision, an RFC3339 stamp, a plain date, or a duration either side of now (`90m ago`, `in 2h`, `+90m`) — and shows it as UTC, as local time, as epoch seconds and milliseconds, and as how long ago or away it is. With --zone, in a third timezone too.\n\nA bare number carries no unit, so the unit is chosen by magnitude and then stated back to you in the `read-as` row: between 1970 and 1973 the ranges genuinely overlap and no rule can resolve that, which makes saying what was assumed part of the answer rather than a footnote.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "when": {
          "default": "now",
          "description": "an instant: now, an epoch number, 2026-09-04T12:00:00Z, 2026-09-04, or a relative duration like \"90m ago\"",
          "type": "string"
        },
        "zone": {
          "description": "also show it in this IANA timezone (Europe/Paris)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "todo_list",
    "description": "── the text below is written by the plugin, not by rta ──\nList tasks\n\nShows top-level tasks by default; pass --parent to list one task's sub-tasks. With --detail: adds creation date and a body preview.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "all": {
          "description": "include completed tasks",
          "type": "boolean"
        },
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "parent": {
          "default": 0,
          "description": "list sub-tasks of this task id instead of top-level tasks",
          "type": "integer"
        },
        "tag": {
          "description": "only tasks with any of these tags",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "todo_search",
    "description": "── the text below is written by the plugin, not by rta ──\nSearch tasks by title, body or tag\n\nSearches every task regardless of parent/child, done or open.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "query": {
          "description": "search terms",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "name": "todo_show",
    "description": "── the text below is written by the plugin, not by rta ──\nShow one task: metadata, description, sub-tasks, references\n\nA composed page rather than one blob: structured metadata (status, due, tags, parent, progress) stays separate from the markdown description, so both stay readable and machine callers can read a due date without parsing prose. Sub-tasks and cross-references (\"#12\" in a body, resolved both ways) follow.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "task id",
          "type": "integer"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "todo_tags",
    "description": "── the text below is written by the plugin, not by rta ──\nList tags in use and how many tasks carry each\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  }
]
