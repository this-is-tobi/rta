[
  {
    "name": "audit_kube_netpol",
    "description": "── the text below is written by the plugin, not by rta ──\nNamespaces with no NetworkPolicy — unrestricted pod-to-pod traffic\n\nEvery non-system namespace checked for at least one NetworkPolicy. With none, the cluster's default applies: every pod in the namespace can reach every other pod, in either direction. Cites CIS Kubernetes Benchmark 2.0.1 5.3.2.\n\nNarrowed to a namespace it checks that one, including a system namespace if you name it explicitly.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "namespace": {
          "description": "audit one namespace instead of the whole cluster",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "audit_kube_podsecurity",
    "description": "── the text below is written by the plugin, not by rta ──\nPods running privileged, in a host namespace, or with root not excluded\n\nEvery pod cluster-wide, checked against three Pod Security Standards controls: a privileged container (fail), hostNetwork/hostPID/hostIPC (fail), and whether anything in the pod asserts non-root — neither the pod nor any container sets runAsNonRoot or a non-zero runAsUser (warn). The non-root check approximates Kubernetes' own securityContext merge rather than fully simulating it: a pod that asserts non-root at the pod level and never overrides it per container is not flagged, which is the common, correct case; one relying only on an image's own non-root USER with no Kubernetes-level assertion at all is. Cites CWE-250 and Kubernetes Pod Security Standards.\n\nA namespace narrows which pods are read, and the clean result says so — it reports that no pod in that namespace is affected, never that no pod in the cluster is.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "namespace": {
          "description": "audit one namespace instead of the whole cluster",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "audit_kube_quotas",
    "description": "── the text below is written by the plugin, not by rta ──\nNamespaces with no ResourceQuota — unbounded resource consumption\n\nEvery non-system namespace checked for at least one ResourceQuota. CIS Kubernetes Benchmark has no numbered control for this — confirmed absent rather than assumed — so this cites NSA/CISA Kubernetes Hardening Guidance's \"Resource policies\" section instead.\n\nNarrowed to a namespace it checks that one, including a system namespace if you name it explicitly — the system-namespace filter exists so a cluster-wide sweep stays quiet, not to refuse a direct question.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "namespace": {
          "description": "audit one namespace instead of the whole cluster",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "audit_kube_rbac",
    "description": "── the text below is written by the plugin, not by rta ──\nOverly-broad RBAC: cluster-admin bindings and wildcard rules\n\nEvery ClusterRoleBinding to cluster-admin, and every Role/ClusterRole rule using a wildcard verb, resource or apiGroup. cluster-admin bound to a broad group (system:authenticated, system:unauthenticated, system:masters) grades fail; bound to anything else grades warn — sometimes a real operator's binding, always worth reading. Cites CIS Kubernetes Benchmark 2.0.1 5.1.1 and 5.1.3.\n\nNarrowed to a namespace it checks that namespace's Roles only. ClusterRoleBindings and ClusterRoles belong to no namespace, so they are not examined at all — reported in the result rather than left implied, since a scoped run that silently skipped the cluster-admin check would read as a clean RBAC posture.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "namespace": {
          "description": "audit one namespace instead of the whole cluster",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "audit_mail",
    "description": "── the text below is written by the plugin, not by rta ──\nAudit a domain's email authentication: SPF, DKIM, DMARC, MTA-STS\n\nAnswers \"can somebody send mail as this domain\" from the records the domain publishes: SPF (present, singular, how it ends, and how close it is to RFC 7208's ten-lookup limit past which it silently stops applying), DMARC (policy, rollout percentage, whether anything reports back), DKIM for a given selector, MTA-STS and TLS-RPT for the server-to-server hop, and MX — including RFC 7505's null MX, which is a hardening measure rather than an omission. Every finding cites an OWASP Top 10:2025 category and a MITRE CWE. With --detail: one section per area plus the cited references with lookup URLs. Read-only, a handful of lookups of names derived from the domain by rule — nothing is enumerated, which is why DKIM needs its selector handed to it.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "domain": {
          "description": "domain to audit",
          "type": "string"
        },
        "selector": {
          "description": "DKIM selector to check (the s= tag of a DKIM-Signature)",
          "type": "string"
        },
        "timeout": {
          "default": 15,
          "description": "lookup timeout in seconds",
          "maximum": 300,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "domain"
      ],
      "type": "object"
    }
  },
  {
    "name": "audit_web",
    "description": "── the text below is written by the plugin, not by rta ──\nAudit a web host: TLS, certificate, headers, cookies, exposure\n\nOne HTTPS request, graded: TLS version and cipher, certificate validity, expiry and signature algorithm, the security headers a host should send (HSTS, CSP — checked for weak directives like unsafe-inline, not just presence — X-Frame-Options/frame-ancestors, cross-origin isolation headers), CORS behavior (probed with an Origin no real browser would send, to catch blind reflection), cookie flags, and the version/stack details a host should not leak. Every finding cites an OWASP Top 10:2025 category and a MITRE CWE, so a result traces back to a named control rather than an opinion. With --detail: one section per area plus the cited references with lookup URLs. Read-only — it only inspects the response the host volunteers, plus the one Origin header the CORS probe adds.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"host\"). You cannot issue one yourself — ask the operator to run `rta grant allow audit.web`.",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "host": {
          "description": "host or URL to audit",
          "type": "string"
        },
        "timeout": {
          "default": 15,
          "description": "request timeout in seconds",
          "maximum": 300,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "host"
      ],
      "type": "object"
    }
  },
  {
    "name": "cert_chain",
    "description": "── the text below is written by the plugin, not by rta ──\nShow the certificate chain presented by a host\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "target": {
          "description": "host[:port] to connect to, or a path to a PEM file (a path on the machine running rta)",
          "type": "string"
        },
        "timeout": {
          "default": 10,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "target"
      ],
      "type": "object"
    }
  },
  {
    "name": "cert_expiry",
    "description": "── the text below is written by the plugin, not by rta ──\nCheck certificate expiry for one or more hosts\n\nSame reasoning as `net probe`: the hosts are the caller's choice, and what a certificate says about itself — subject, issuer, DNS names — is read as tool output the same way a banner is. Needs a grant, one per host listed.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"targets\"). You cannot issue one yourself — ask the operator to run `rta grant allow cert.expiry`.",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "targets": {
          "description": "hosts to check (host[:port])",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "default": 10,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        },
        "warn-days": {
          "default": 30,
          "description": "flag certificates expiring within this many days",
          "type": "integer"
        }
      },
      "required": [
        "targets"
      ],
      "type": "object"
    }
  },
  {
    "name": "cert_inspect",
    "description": "── the text below is written by the plugin, not by rta ──\nShow certificate details for a host or PEM file\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "target": {
          "description": "host[:port] to connect to, or a path to a PEM file (a path on the machine running rta)",
          "type": "string"
        },
        "timeout": {
          "default": 10,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "target"
      ],
      "type": "object"
    }
  },
  {
    "name": "cert_pem",
    "description": "── the text below is written by the plugin, not by rta ──\nPrint the certificate chain as PEM\n\nThe bytes, not a description of them. `cert chain` draws what a host presents so a person can read it; this hands the same certificates back in the form every other tool takes one — a Kubernetes ConfigMap, a Dockerfile COPY, `update-ca-certificates`, a paste into somebody's terminal.\n\n--include issuers is the one to reach for behind a private CA: it drops the leaf and leaves the chain that has to be *trusted*, which is what a ca-bundle is. chain (the default) is everything the host presented, leaf is the end-entity certificate alone.\n\nA presented chain is what the host chose to send and is not always complete — a server that omits its intermediate presents a leaf that validates nowhere else, and this reports what arrived rather than filling the gap from a trust store, because a bundle that silently differs from what the server serves is how a working local test hides a broken deployment.\n\nRead, and it stays read from anywhere but a terminal: --out names a path on *this* machine, so it is a person's flag only and an MCP caller always gets the PEM back in the response.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "include": {
          "default": "chain",
          "description": "which certificates to print",
          "enum": [
            "chain",
            "issuers",
            "leaf"
          ],
          "type": "string"
        },
        "target": {
          "description": "host[:port] to connect to, or a path to a PEM file (a path on the machine running rta)",
          "type": "string"
        },
        "timeout": {
          "default": 10,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "target"
      ],
      "type": "object"
    }
  },
  {
    "name": "cert_tls",
    "description": "── the text below is written by the plugin, not by rta ──\nReport the negotiated TLS version and cipher for a host\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "target": {
          "description": "host[:port] to connect to, or a path to a PEM file (a path on the machine running rta)",
          "type": "string"
        },
        "timeout": {
          "default": 10,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "target"
      ],
      "type": "object"
    }
  },
  {
    "name": "codec_b64",
    "description": "── the text below is written by the plugin, not by rta ──\nBase64 encode or decode a value\n\nDecoding accepts standard, URL-safe, and unpadded variants without being told which — the caller already has the encoded value, so being forgiving about which base64 dialect produced it costs nothing.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "decode": {
          "description": "decode instead of encode",
          "type": "boolean"
        },
        "url": {
          "description": "use the URL-safe alphabet when encoding",
          "type": "boolean"
        },
        "value": {
          "description": "the text to transform",
          "type": "string"
        }
      },
      "required": [
        "value"
      ],
      "type": "object"
    }
  },
  {
    "name": "codec_hex",
    "description": "── the text below is written by the plugin, not by rta ──\nHex encode or decode a value\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "decode": {
          "description": "decode instead of encode",
          "type": "boolean"
        },
        "value": {
          "description": "the text to transform",
          "type": "string"
        }
      },
      "required": [
        "value"
      ],
      "type": "object"
    }
  },
  {
    "name": "codec_jwt",
    "description": "── the text below is written by the plugin, not by rta ──\nDecode a JWT's header and claims for inspection\n\nUnverified by default and clearly labeled as such: this is for reading a token while debugging, not for authenticating one. No signature check is performed — anyone can hand you a token with any claims at all.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "token": {
          "description": "the JWT to decode",
          "type": "string"
        }
      },
      "required": [
        "token"
      ],
      "type": "object"
    }
  },
  {
    "name": "codec_url",
    "description": "── the text below is written by the plugin, not by rta ──\nURL-escape or unescape a value\n\nQuery-component escaping (spaces become +), the form almost everyone means by \"URL encode this\" — the value for a query string or form body.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "decode": {
          "description": "decode instead of encode",
          "type": "boolean"
        },
        "value": {
          "description": "the text to transform",
          "type": "string"
        }
      },
      "required": [
        "value"
      ],
      "type": "object"
    }
  },
  {
    "name": "debug_ansi",
    "description": "── the text below is written by the plugin, not by rta ──\nBreak down the ANSI escape sequences in a string, byte by byte\n\nWalks the input the way a terminal would, one printable run or control/escape sequence at a time, and names what each one does — SGR colors and attributes, cursor movement, screen/line erase, and the OSC sequences most worth knowing about on sight: window title, hyperlink target, and the system clipboard write (decoded, not left as base64). Never prints a raw control byte back at the terminal it is running in — the whole point is seeing what a sequence does without it happening.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "input": {
          "description": "text containing escape sequences; reads piped stdin when omitted",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "eol_check",
    "description": "── the text below is written by the plugin, not by rta ──\nWhether a product, or one of its release cycles, is still supported\n\nLooks up a product on endoflife.date and grades every release cycle by how close it is to its own end-of-life date. Name one cycle to see just that row; leave it out to see all of them. Aliases work — \"postgres\" and \"postgresql\" name the same product.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "cycle": {
          "description": "one release cycle, e.g. 15, bookworm, 22.04 — every cycle is shown when omitted",
          "type": "string"
        },
        "product": {
          "description": "product name or alias — see https://endoflife.date for the catalogue",
          "type": "string"
        },
        "warn-days": {
          "default": 90,
          "description": "flag a cycle within this many days of its end-of-life date",
          "type": "integer"
        }
      },
      "required": [
        "product"
      ],
      "type": "object"
    }
  },
  {
    "name": "eol_products",
    "description": "── the text below is written by the plugin, not by rta ──\nSearch the endoflife.date catalogue: names, aliases and categories\n\nLists the products endoflife.date tracks, with the aliases eol.check and eol.watch accept for each. A term narrows the list to products whose name, label or alias contains it; leave it out to see the whole catalogue.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "category": {
          "description": "only this category — database, os, framework, lang, …",
          "type": "string"
        },
        "term": {
          "description": "part of a name, label or alias — postgres, kube, ubuntu",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "eol_watch",
    "description": "── the text below is written by the plugin, not by rta ──\nGrade every product and cycle on a configured watchlist at once\n\nRuns eol.check over a list of products, each optionally pinned to one release cycle as product/cycle — postgresql/15, nodejs, debian/bookworm. Write the list once as `plugins: eol: products:` in your config or a profile and it is what this grades from then on. A product the API does not know, or a cycle the product does not have, is one row saying so; the rest of the list is still graded.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "products": {
          "description": "product or product/cycle, repeatable — usually from `plugins: eol: products:` in your config",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "warn-days": {
          "default": 90,
          "description": "flag a cycle within this many days of its end-of-life date",
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "gen_overview",
    "description": "── the text below is written by the plugin, not by rta ──\nA sampler of the shapes worth generating, ready to use\n\nGenerates one of each common shape side by side — passwords, encryption key material, URL-safe and TOTP tokens, UUIDs — labelled with what it is for, the entropy it actually carries and the command that reproduces it, so picking the right shape does not require remembering the flags first. With --detail: every preset, plus why a \"32-character key\" is not a 32-byte one. Every value is real and freshly generated; nothing is an example.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "gen_password",
    "description": "── the text below is written by the plugin, not by rta ──\nGenerate a cryptographically random password\n\ncrypto/rand only, with unbiased alphabet selection (rand.Int, never rand.Intn or modulo, which biases low indices unless the alphabet size divides the RNG range evenly). Reports entropy in bits: length x log2(alphabet size).\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "count": {
          "default": 1,
          "description": "how many to generate",
          "type": "integer"
        },
        "exclude-ambiguous": {
          "description": "drop look-alike characters: 0O1lI",
          "type": "boolean"
        },
        "length": {
          "default": 20,
          "description": "characters",
          "maximum": 4096,
          "minimum": 1,
          "type": "integer"
        },
        "no-digits": {
          "description": "exclude digits",
          "type": "boolean"
        },
        "no-lower": {
          "description": "exclude lowercase letters",
          "type": "boolean"
        },
        "no-upper": {
          "description": "exclude uppercase letters",
          "type": "boolean"
        },
        "symbols": {
          "description": "include symbols: !@#$%^\u0026*()-_=+[]{}\u003c\u003e?",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "gen_token",
    "description": "── the text below is written by the plugin, not by rta ──\nGenerate random bytes, formatted as hex/base64/base32\n\ncrypto/rand bytes, formatted with --encoding. base32 at the right length is already a TOTP secret (RFC 4648/6238) — there is no separate gen.totp.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "encoding": {
          "default": "hex",
          "description": "output encoding",
          "enum": [
            "hex",
            "base64",
            "base64url",
            "base32"
          ],
          "type": "string"
        },
        "length": {
          "default": 32,
          "description": "bytes of entropy",
          "maximum": 4096,
          "minimum": 1,
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "gen_uuid",
    "description": "── the text below is written by the plugin, not by rta ──\nGenerate a UUID: v4 random (default) or v7 time-ordered\n\nv1/v3/v5/v6 are deliberately not offered: v1 leaks the generating machine's MAC address and a timestamp, v3/v5 hash a namespace+name instead of generating anything (a different operation), and v6 is superseded by v7 for the same time-ordering goal, per RFC 9562's own recommendation.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "count": {
          "default": 1,
          "description": "how many to generate",
          "type": "integer"
        },
        "version": {
          "default": "4",
          "description": "UUID version",
          "enum": [
            "4",
            "7"
          ],
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "http_delete",
    "description": "── the text below is written by the plugin, not by rta ──\nDELETE a URL\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"url\"). You cannot issue one yourself — ask the operator to run `rta grant allow http.delete`.",
    "destructive_hint": false,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "basic": {
          "description": "basic auth as user:password",
          "type": "string"
        },
        "bearer": {
          "description": "bearer token (Authorization: Bearer ...)",
          "type": "string"
        },
        "header": {
          "description": "request header, repeatable: -H 'Key: Value'",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "default": 30,
          "description": "request timeout in seconds",
          "maximum": 600,
          "minimum": 1,
          "type": "integer"
        },
        "url": {
          "description": "request URL",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    }
  },
  {
    "name": "http_get",
    "description": "── the text below is written by the plugin, not by rta ──\nGET a URL and show status, timing and body\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"url\"). You cannot issue one yourself — ask the operator to run `rta grant allow http.get`.",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "basic": {
          "description": "basic auth as user:password",
          "type": "string"
        },
        "bearer": {
          "description": "bearer token (Authorization: Bearer ...)",
          "type": "string"
        },
        "header": {
          "description": "request header, repeatable: -H 'Key: Value'",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "default": 30,
          "description": "request timeout in seconds",
          "maximum": 600,
          "minimum": 1,
          "type": "integer"
        },
        "url": {
          "description": "request URL",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    }
  },
  {
    "name": "http_head",
    "description": "── the text below is written by the plugin, not by rta ──\nHEAD a URL and show status, timing and headers\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"url\"). You cannot issue one yourself — ask the operator to run `rta grant allow http.head`.",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "basic": {
          "description": "basic auth as user:password",
          "type": "string"
        },
        "bearer": {
          "description": "bearer token (Authorization: Bearer ...)",
          "type": "string"
        },
        "header": {
          "description": "request header, repeatable: -H 'Key: Value'",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "default": 30,
          "description": "request timeout in seconds",
          "maximum": 600,
          "minimum": 1,
          "type": "integer"
        },
        "url": {
          "description": "request URL",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    }
  },
  {
    "name": "http_post",
    "description": "── the text below is written by the plugin, not by rta ──\nPOST to a URL with an optional body\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"url\"). You cannot issue one yourself — ask the operator to run `rta grant allow http.post`.",
    "destructive_hint": false,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "basic": {
          "description": "basic auth as user:password",
          "type": "string"
        },
        "bearer": {
          "description": "bearer token (Authorization: Bearer ...)",
          "type": "string"
        },
        "data": {
          "description": "request body, sent as given",
          "type": "string"
        },
        "header": {
          "description": "request header, repeatable: -H 'Key: Value'",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "default": 30,
          "description": "request timeout in seconds",
          "maximum": 600,
          "minimum": 1,
          "type": "integer"
        },
        "url": {
          "description": "request URL",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    }
  },
  {
    "name": "http_put",
    "description": "── the text below is written by the plugin, not by rta ──\nPUT to a URL with an optional body\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"url\"). You cannot issue one yourself — ask the operator to run `rta grant allow http.put`.",
    "destructive_hint": false,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "basic": {
          "description": "basic auth as user:password",
          "type": "string"
        },
        "bearer": {
          "description": "bearer token (Authorization: Bearer ...)",
          "type": "string"
        },
        "data": {
          "description": "request body, sent as given",
          "type": "string"
        },
        "header": {
          "description": "request header, repeatable: -H 'Key: Value'",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "default": 30,
          "description": "request timeout in seconds",
          "maximum": 600,
          "minimum": 1,
          "type": "integer"
        },
        "url": {
          "description": "request URL",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    }
  },
  {
    "name": "http_status",
    "description": "── the text below is written by the plugin, not by rta ──\nWhat an HTTP status code means, and where it is defined\n\nA code prints its card; a class (`4xx`, `500-599`) or nothing prints the table. Offline: the text is the standard's, and every code net/http knows is here.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "code": {
          "description": "a code (404), a class (4xx) or a range (500-599); every code when omitted",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "kv_env",
    "description": "── the text below is written by the plugin, not by rta ──\nPrint stored values as shell exports\n\nFor `eval \"$(rta kv env db-password)\"` — loads secrets into a shell session without ever writing them to a file. Key names become environment names (db-password → DB_PASSWORD). --format dotenv writes .env syntax instead. Same grant requirement as kv.get: this reveals values. Naming no key means every key, which is a wider ask and needs a grant for kv.env itself rather than one per key.\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"key\"). You cannot issue one yourself — ask the operator to run `rta grant allow kv.env`.",
    "destructive_hint": false,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "format": {
          "default": "export",
          "description": "shell exports, or .env syntax",
          "enum": [
            "export",
            "dotenv"
          ],
          "type": "string"
        },
        "key": {
          "description": "keys to export (default: all)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "prefix": {
          "description": "prepend this to every variable name, e.g. APP_",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "kv_get",
    "description": "── the text below is written by the plugin, not by rta ──\nReveal a stored value\n\nWrites the value to stdout with no quoting and no framing, so `rta kv get gh-token | gh auth login --with-token` is a pipe and not a transformation. The pretty renderer terminates the line, so a value stored without a trailing newline gains one on the way out; for the byte-exact copy use --out, which writes the stored bytes to a file with 0600 instead, and is how a certificate or key goes back to disk without passing through your scrollback. To use a secret without seeing it at all, `kv copy` puts it on the clipboard and prints nothing.\n\nClassified as a write because revealing a secret is the sensitive act. An MCP agent therefore needs --allow-write, and on top of that a per-key grant issued by a person (`rta grant allow kv.get \u003ckey\u003e --ttl 15m`). --out is a person's flag only, since a grant authorizes revealing a value, not choosing where on this machine it gets written; an MCP caller always gets the value back in the response.\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"key\"). You cannot issue one yourself — ask the operator to run `rta grant allow kv.get`.",
    "destructive_hint": false,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "key": {
          "description": "key to reveal",
          "type": "string"
        }
      },
      "required": [
        "key"
      ],
      "type": "object"
    }
  },
  {
    "name": "kv_history",
    "description": "── the text below is written by the plugin, not by rta ──\nWhat a key held before, and when each value was replaced\n\nEvery write over an existing key keeps what it replaced — the last 5 values, inside the same encrypted store — and `kv rm` keeps the whole entry aside rather than destroying it. This lists that past: kind, size, description, when each value was set and when it was replaced. Never the values themselves, which is what keeps it a Read; `kv restore` brings one back.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "key": {
          "description": "key to look back on",
          "type": "string"
        }
      },
      "required": [
        "key"
      ],
      "type": "object"
    }
  },
  {
    "name": "kv_init",
    "description": "── the text below is written by the plugin, not by rta ──\nSet up how the store is encrypted\n\nChooses the lock once, so nothing has to be repeated afterwards. --generate makes a dedicated age key for this store and uses it: no passphrase to type, no flags to remember, and — unlike your SSH login key — a key whose loss costs you this store and nothing else. --identity locks it to a key you already have (age or SSH). --recipient adds other readers.\n\nA passphrase store needs no init: that is what you get by default.\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".",
    "destructive_hint": false,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "generate": {
          "description": "create a dedicated age key for this store and lock it to that",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "kv_list",
    "description": "── the text below is written by the plugin, not by rta ──\nList stored keys (names and metadata only, never values)\n\nNever returns a value or a preview of one — only key names, what kind of thing each is, its size, its description and when it changed. With --detail: the source filename of anything stored from disk.\n\n--match is the \"which one was it called?\" filter: a substring of the name or the description, case-insensitive, so `kv list --match aws` finds `prod-deploy-key` when the description is the only place the word AWS appears.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "kind": {
          "description": "only entries of this kind",
          "enum": [
            "string",
            "json",
            "certificate",
            "private-key",
            "public-key",
            "ssh-key",
            "file"
          ],
          "type": "string"
        },
        "match": {
          "description": "only keys whose name or description contains this (case-insensitive)",
          "type": "string"
        },
        "removed": {
          "description": "list what `kv rm` set aside instead — restorable until purged",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "kv_recipients",
    "description": "── the text below is written by the plugin, not by rta ──\nList the public keys that can decrypt the store\n\nAnswers \"who can read this?\" without unlocking anything — recipients are public keys, so the list is plaintext by design. An empty list means the store is passphrase-encrypted.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "kv_rekey",
    "description": "── the text below is written by the plugin, not by rta ──\nChange which keys can open the store\n\nThe store is decrypted and written back under a new set of keys, so this is the one command that changes who can read what you already stored.\n\nAdding is the default: `--generate` makes a dedicated age key and leaves the existing readers alone, which is how a store locked to your SSH key gains a key that needs no passphrase — after which both open it and the new one is found without a flag. `--only` makes the set exclusive instead: `--generate --only` switches the lock from one key to the other, and naming the readers you keep is how a reader is removed.\n\n--identity never changes the set: it says which private key is here, which is what opens the store and the only way to prove a key is yours — a public key on its own shows nothing of the sort.\n\nTwo things are refused rather than confirmed. Reading comes first, so a store you cannot open is a store you cannot re-key. And a key you hold has to survive the change: handing the store to somebody else and locking yourself out of it in the same keystroke is not a thing to be sure about.\n\nOld copies of the file stay readable by the old keys — re-keying changes the lock, it does not reach into backups.\n── end of plugin-written text ──\n\nSafety: destructive. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability. You cannot issue one yourself — ask the operator to run `rta grant allow kv.rekey`.",
    "destructive_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "generate": {
          "description": "create a dedicated age key for this store and add it",
          "type": "boolean"
        },
        "only": {
          "description": "lock it to exactly these keys, dropping every other reader",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "kv_rename",
    "description": "── the text below is written by the plugin, not by rta ──\nRename a key, keeping its value and its history\n\nRenaming used to mean `kv get` piped into `kv set` and then `kv rm`: two grants for an operation that reveals nothing, and the secret itself sitting in shell history at the join. This moves the entry inside the store — the value is never decrypted into anything but memory, and its description, kind, source and timestamps travel with it.\n\nA name that is already taken is refused rather than overwritten: renaming onto an existing key would destroy the secret in it, which is `kv rm`'s question and is asked with `kv rm`'s answer.\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"key\"). You cannot issue one yourself — ask the operator to run `rta grant allow kv.rename`.",
    "destructive_hint": false,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "key": {
          "description": "key to rename",
          "type": "string"
        },
        "new-name": {
          "description": "what to call it instead",
          "type": "string"
        }
      },
      "required": [
        "key",
        "new-name"
      ],
      "type": "object"
    }
  },
  {
    "name": "kv_restore",
    "description": "── the text below is written by the plugin, not by rta ──\nBring back a removed key, or an earlier value of one\n\nWith no --revision, a key `kv rm` removed comes back exactly as it was, history included. With one, the key's current value is replaced by that earlier one — the number `kv history` lists, 1 being the most recent — and the value being replaced joins the history in turn, so a restore is itself undoable.\n\nA removed key whose name has since been reused is refused rather than merged: rename the live one first, or purge the removed one.\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"key\"). You cannot issue one yourself — ask the operator to run `rta grant allow kv.restore`.",
    "destructive_hint": false,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "key": {
          "description": "key to restore",
          "type": "string"
        },
        "revision": {
          "description": "an earlier value to restore, as `kv history` numbers them; omit to restore a removed key",
          "type": "integer"
        }
      },
      "required": [
        "key"
      ],
      "type": "object"
    }
  },
  {
    "name": "kv_rm",
    "description": "── the text below is written by the plugin, not by rta ──\nRemove a stored key, keeping it restorable until purged\n\nThe key leaves the listing and every read of it, but the entry is kept aside whole — value, history and all — inside the same encrypted store, where `kv list --removed` shows it and `kv restore` brings it back. That is the undo a mis-click needs. --purge is the removal that has none: the entry and everything it ever held are gone when it returns, and it also finishes off a key removed earlier.\n\nDestructive either way, because a removed key is still a key nothing can read until somebody notices. Who can open the store does not change: removing the last entry leaves an empty store locked to the same keys, not an unlocked one. To rename rather than replace, `kv rename` moves an entry without its value ever leaving memory.\n── end of plugin-written text ──\n\nSafety: destructive. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"key\"). You cannot issue one yourself — ask the operator to run `rta grant allow kv.rm`.",
    "destructive_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "key": {
          "description": "key to remove",
          "type": "string"
        },
        "purge": {
          "description": "destroy it, history included — no restore",
          "type": "boolean"
        }
      },
      "required": [
        "key"
      ],
      "type": "object"
    }
  },
  {
    "name": "kv_set",
    "description": "── the text below is written by the plugin, not by rta ──\nSet (or overwrite) a stored value\n\nThe value comes from the argument or from --file. The kind (certificate, private key, json, file, string) is detected from the content unless --kind says otherwise. Writing an entry never changes who can read the store: that is `kv rekey`, which is destructive for the reason this is not.\n\nWith no value at all, --description and --kind relabel an entry that already exists, leaving the secret and both timestamps untouched — so correcting what something is for does not mean fetching and re-typing the secret itself, and does not reset the age `kv list` reports for it.\n\nSetting a key that already exists replaces the secret in it and keeps the old one — the last 5 values stay behind the key, listed by `kv history` and brought back by `kv restore --revision`. So a paste over the wrong key is a mistake you undo, not one you re-type from memory. Over MCP it still needs a per-key grant: an agent that can overwrite a secret can still break what reads it, undo or not.\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"key\"). You cannot issue one yourself — ask the operator to run `rta grant allow kv.set`.",
    "destructive_hint": false,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "description": {
          "description": "what this is for — shown by kv list",
          "type": "string"
        },
        "key": {
          "description": "key to set",
          "type": "string"
        },
        "kind": {
          "description": "override the kind detected from the content",
          "enum": [
            "string",
            "json",
            "certificate",
            "private-key",
            "public-key",
            "ssh-key",
            "file"
          ],
          "type": "string"
        },
        "value": {
          "description": "value to store",
          "type": "string"
        }
      },
      "required": [
        "key"
      ],
      "type": "object"
    }
  },
  {
    "name": "kv_show",
    "description": "── the text below is written by the plugin, not by rta ──\nShow everything about one entry except its value\n\nThe detail page for a stored key: what kind of thing it is, what it is for, how big it is, where it came from and when it changed. Deliberately not the value — that is `kv get` (prints it) or `kv copy` (does not), both writes for exactly that reason. This stays Read because everything on it is metadata you can safely put on a screen.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "key": {
          "description": "key to describe",
          "type": "string"
        }
      },
      "required": [
        "key"
      ],
      "type": "object"
    }
  },
  {
    "name": "kv_tree",
    "description": "── the text below is written by the plugin, not by rta ──\nThe store as a tree of the folders its keys share\n\nKeys named with slashes — `staging/db/password`, `prod/deploy-key` — are drawn as the folders they share, each leaf labelled with its kind. Names only, never values, the same line `kv list` draws. `rta grant allow kv.get staging/` is how one of those folders becomes a grant's scope, so this is also the map of what such a grant covers.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "net_dns",
    "description": "── the text below is written by the plugin, not by rta ──\nResolve DNS records for a name, or a name for an address\n\n--type auto (the default) asks the question you usually mean: A, AAAA and CNAME for a name, PTR for an address. --server sends the query to a specific resolver instead of the system one, which is how you tell \"the record is wrong\" apart from \"my resolver is stale\". With --detail: the query, the resolver that answered, and how long it took.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "name": {
          "description": "name or IP address to resolve",
          "type": "string"
        },
        "server": {
          "description": "resolver to query, e.g. 1.1.1.1 or 9.9.9.9:53 (default: the system resolver)",
          "type": "string"
        },
        "timeout": {
          "default": 5,
          "description": "query timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        },
        "type": {
          "default": "auto",
          "description": "which record type to ask for",
          "enum": [
            "auto",
            "A",
            "AAAA",
            "CNAME",
            "MX",
            "NS",
            "TXT",
            "PTR",
            "SRV"
          ],
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "name": "net_ping",
    "description": "── the text below is written by the plugin, not by rta ──\nPing a host and report latency statistics\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "count": {
          "default": 4,
          "description": "number of probes",
          "maximum": 100,
          "minimum": 1,
          "type": "integer"
        },
        "graph": {
          "description": "plot per-probe latency instead of summary stats",
          "type": "boolean"
        },
        "host": {
          "description": "host to ping",
          "type": "string"
        },
        "timeout": {
          "default": 10,
          "description": "overall timeout in seconds",
          "maximum": 300,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "host"
      ],
      "type": "object"
    }
  },
  {
    "name": "net_port",
    "description": "── the text below is written by the plugin, not by rta ──\nTCP connect-scan ports on a host\n\nWhich ports answer, not what they say — `net probe` shows the banner for one; this maps many at once. Needs a grant for the same reason: the host is the caller's choice.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"host\"). You cannot issue one yourself — ask the operator to run `rta grant allow net.port`.",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "host": {
          "description": "host to scan",
          "type": "string"
        },
        "ports": {
          "default": "22,80,443",
          "description": "comma-separated ports and ranges, e.g. 22,80,8000-8010 (up to 4096 at a time)",
          "type": "string"
        },
        "timeout": {
          "default": 2,
          "description": "per-port timeout in seconds",
          "maximum": 60,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "host"
      ],
      "type": "object"
    }
  },
  {
    "name": "net_probe",
    "description": "── the text below is written by the plugin, not by rta ──\nOpen a TCP connection and report what answers\n\nWhat telnet \u003chost\u003e \u003cport\u003e is really used for: prove the port accepts a connection, time the handshake, and show the banner the service volunteers. --tls completes a TLS handshake first and reports the negotiated version and cipher. It only ever listens; to speak first — the protocols that expect the client to open — use `net send`, which is a write. Both need a grant: the destination is the caller's choice either way, and whatever it says back is read as tool output.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"host\"). You cannot issue one yourself — ask the operator to run `rta grant allow net.probe`.",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "host": {
          "description": "host to connect to",
          "type": "string"
        },
        "port": {
          "description": "port to connect to",
          "maximum": 65535,
          "minimum": 1,
          "type": "integer"
        },
        "timeout": {
          "default": 5,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        },
        "tls": {
          "description": "complete a TLS handshake after connecting",
          "type": "boolean"
        },
        "wait": {
          "default": 2,
          "description": "seconds to wait for the service to say something",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "host",
        "port"
      ],
      "type": "object"
    }
  },
  {
    "name": "net_send",
    "description": "── the text below is written by the plugin, not by rta ──\nSend bytes to a TCP port and report what comes back\n\nOpens a TCP connection, writes what you give it, and shows the reply — how you poke a protocol that expects the client to speak first (SMTP, Redis, HTTP/1.0). \\r\\n and \\n in --data are interpreted. --tls speaks it over TLS. This is a write: what arrives is executed by whatever is listening, so it needs an explicit grant before an AI agent can use it. `net probe` is the read-only half — connect, handshake, listen — and carries the same grant, since the destination is still the caller's to choose either way.\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"host\"). You cannot issue one yourself — ask the operator to run `rta grant allow net.send`.",
    "destructive_hint": false,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "data": {
          "description": "bytes to send, e.g. \"GET / HTTP/1.0\\r\\n\\r\\n\"",
          "type": "string"
        },
        "host": {
          "description": "host to connect to",
          "type": "string"
        },
        "port": {
          "description": "port to connect to",
          "maximum": 65535,
          "minimum": 1,
          "type": "integer"
        },
        "timeout": {
          "default": 5,
          "description": "connect timeout in seconds",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        },
        "tls": {
          "description": "complete a TLS handshake before sending",
          "type": "boolean"
        },
        "wait": {
          "default": 2,
          "description": "seconds to wait for a reply",
          "maximum": 120,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "host",
        "port",
        "data"
      ],
      "type": "object"
    }
  },
  {
    "name": "net_trace",
    "description": "── the text below is written by the plugin, not by rta ──\nTrace the route to a host, hop by hop\n\nICMP echo probes with a rising TTL: each hop that expires one reports itself. Answers the question ping cannot — not just \"is it reachable\" but \"where does it stop\". Silent hops show as *; that is a router declining to reply, not necessarily a fault.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "host": {
          "description": "host to trace",
          "type": "string"
        },
        "max-hops": {
          "default": 30,
          "description": "stop after this many hops",
          "maximum": 64,
          "minimum": 1,
          "type": "integer"
        },
        "probes": {
          "default": 3,
          "description": "probes per hop",
          "maximum": 10,
          "minimum": 1,
          "type": "integer"
        },
        "resolve": {
          "default": true,
          "description": "reverse-resolve hop addresses to names",
          "type": "boolean"
        },
        "timeout": {
          "default": 2,
          "description": "per-probe timeout in seconds",
          "maximum": 60,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "host"
      ],
      "type": "object"
    }
  },
  {
    "name": "note_add",
    "description": "── the text below is written by the plugin, not by rta ──\nAdd a note\n\nA title is enough. --todo (or a due date, which implies it) makes it something to do; a parent makes it part of something bigger; markdown in the body is rendered on human surfaces.\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".",
    "destructive_hint": false,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "body": {
          "description": "note content, markdown supported",
          "type": "string"
        },
        "due": {
          "description": "due date: today, tomorrow, a weekday, or 2006-01-02",
          "type": "string"
        },
        "parent": {
          "description": "make this a sub-note of the given note id",
          "type": "integer"
        },
        "tag": {
          "description": "tags, repeatable (--tag backend --tag urgent)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "note title",
          "type": "string"
        },
        "todo": {
          "description": "make it a to-do: something to check off",
          "type": "boolean"
        }
      },
      "required": [
        "title"
      ],
      "type": "object"
    }
  },
  {
    "name": "note_done",
    "description": "── the text below is written by the plugin, not by rta ──\nCheck a note off\n\nDone for a task, filed for a note: either way it leaves the default list and stays findable under --all and in search.\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".",
    "destructive_hint": false,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "note id",
          "type": "integer"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "note_edit",
    "description": "── the text below is written by the plugin, not by rta ──\nEdit a note's title, body, tags, due date or parent\n\nEmpty fields keep their current value. --tag - clears all tags; --due none clears the due date.\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".",
    "destructive_hint": false,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "body": {
          "description": "new body, markdown supported (empty keeps the current one)",
          "type": "string"
        },
        "due": {
          "description": "due date: today, tomorrow, a weekday, or 2006-01-02",
          "type": "string"
        },
        "id": {
          "description": "note id",
          "type": "integer"
        },
        "parent": {
          "default": -1,
          "description": "re-parent to this note id (0 makes it top-level)",
          "type": "integer"
        },
        "tag": {
          "description": "tags, repeatable (--tag backend --tag urgent)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "new title (empty keeps the current one)",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "note_list",
    "description": "── the text below is written by the plugin, not by rta ──\nList notes\n\nOpen notes, the ones with a due date first — soonest on top — then the rest in the order they were written. Checked-off notes are hidden until --all. Shows top-level notes by default; pass --parent to list one note's sub-notes. With --detail: adds creation date and a body preview.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "all": {
          "description": "include checked-off notes",
          "type": "boolean"
        },
        "detail": {
          "default": false,
          "description": "return the full detailed view instead of the compact summary",
          "type": "boolean"
        },
        "parent": {
          "default": 0,
          "description": "list sub-notes of this note id instead of top-level notes",
          "type": "integer"
        },
        "tag": {
          "description": "only notes with any of these tags",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "note_reopen",
    "description": "── the text below is written by the plugin, not by rta ──\nUn-check a note\n\nThe undo for `note done`. Checking off the wrong note is a one-keystroke mistake, and a list you cannot take something back out of is a list people stop trusting. Re-opening an already-open note is a no-op, not an error.\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".",
    "destructive_hint": false,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "note id",
          "type": "integer"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "note_rm",
    "description": "── the text below is written by the plugin, not by rta ──\nRemove a note permanently\n\nSub-notes are re-parented to the removed note's parent, never deleted silently.\n── end of plugin-written text ──\n\nSafety: destructive. Returns a JSON view envelope discriminated by \"type\".\n\nRequires a grant a person issued for this capability (optionally narrowed to one \"id\"). You cannot issue one yourself — ask the operator to run `rta grant allow note.rm`.",
    "destructive_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "note id",
          "type": "integer"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "note_search",
    "description": "── the text below is written by the plugin, not by rta ──\nSearch notes by title, body or tag\n\nSearches every note regardless of parent/child, done or open.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "query": {
          "description": "search terms",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "name": "note_show",
    "description": "── the text below is written by the plugin, not by rta ──\nShow one note: metadata, content, sub-notes, references\n\nA composed page rather than one blob: structured metadata (status, due, tags, parent, progress) stays separate from the markdown content, so both stay readable and machine callers can read a due date without parsing prose. Sub-notes and cross-references (\"#12\" in a body, resolved both ways) follow.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "note id",
          "type": "integer"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "note_tags",
    "description": "── the text below is written by the plugin, not by rta ──\nList tags in use and how many notes carry each\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "note_toggle",
    "description": "── the text below is written by the plugin, not by rta ──\nTurn a note into a to-do, or a to-do back into a note\n\nThe switch between the two kinds of thing in the notebook. A note becomes an open to-do; a to-do becomes a note and forgets whether it was done, which is the point — a note has nothing to be done about.\n── end of plugin-written text ──\n\nSafety: write. Returns a JSON view envelope discriminated by \"type\".",
    "destructive_hint": false,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "note id",
          "type": "integer"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "time_at",
    "description": "── the text below is written by the plugin, not by rta ──\nConvert an instant between epoch, UTC, local time and a named zone\n\nTakes an instant in whatever form you have it — an epoch number at any precision, an RFC3339 stamp, a plain date, or a duration either side of now (`90m ago`, `in 2h`, `+90m`) — and shows it as UTC, as local time, as epoch seconds and milliseconds, and as how long ago or away it is. With --zone, in a third timezone too.\n\nA bare number carries no unit, so the unit is chosen by magnitude and then stated back to you in the `read-as` row: between 1970 and 1973 the ranges genuinely overlap and no rule can resolve that, which makes saying what was assumed part of the answer rather than a footnote.\n── end of plugin-written text ──\n\nSafety: read. Returns a JSON view envelope discriminated by \"type\".",
    "read_only_hint": true,
    "idempotent_hint": true,
    "input_schema": {
      "additionalProperties": false,
      "properties": {
        "when": {
          "default": "now",
          "description": "an instant: now, an epoch number, 2026-09-04T12:00:00Z, 2026-09-04, or a relative duration like \"90m ago\"",
          "type": "string"
        },
        "zone": {
          "description": "also show it in this IANA timezone (Europe/Paris)",
          "type": "string"
        }
      },
      "type": "object"
    }
  }
]
