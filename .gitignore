# Binaries
/rta
/dist/
/bin/

# Test artifacts
coverage.out
*.test

# Editor / OS
.DS_Store
.idea/
.images/

# Working documents: the blueprint, the decision records, the backlogs. They
# are how this gets built and not how it gets used, and the release tarball
# has always shipped docs/ alone for that reason — this is the same rule
# applied to the repository, which was shipping all of it.
.local/
